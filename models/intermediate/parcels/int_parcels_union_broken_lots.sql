{{
    config(
        tags=['parcels', 'monthly']
    )
}}

select
    min(obj_id) as obj_id,
    parcel_id,
    any_value(site_parcel_id) as site_parcel_id,
    any_value(parcel_address) as parcel_address,
    any_value(date_parcel_changed) as date_parcel_changed,
    any_value(property_class) as property_class,
    any_value(property_use) as property_use,
    any_value(assessment_area) as assessment_area,
    any_value(area_name) as area_name,
    any_value(more_than_one_build) as more_than_one_build,
    any_value(home_style) as home_style,
    any_value(year_built) as year_built,
    any_value(bedrooms) as bedrooms,
    any_value(full_baths) as full_baths,
    any_value(half_baths) as half_baths,
    any_value(total_living_area) as total_living_area,
    any_value(first_floor) as first_floor,
    any_value(second_floor) as second_floor,
    any_value(third_floor) as third_floor,
    any_value(above_third_floor) as above_third_floor,
    any_value(finished_attic) as finished_attic,
    any_value(basement) as basement,
    any_value(finished_basement) as finished_basement,
    any_value(exterior_wall_1) as exterior_wall_1,
    any_value(exterior_wall_2) as exterior_wall_2,
    any_value(fireplaces) as fireplaces,
    any_value(central_air) as central_air,
    any_value(partial_assessed) as partial_assessed,
    any_value(assessed_by_state) as assessed_by_state,
    any_value(current_land_value) as current_land_value,
    any_value(current_improvement_value) as current_improvement_value,
    any_value(current_total_value) as current_total_value,
    any_value(previous_land_value) as previous_land_value,
    any_value(previous_improvement_value) as previous_improvement_value,
    any_value(previous_total_value) as previous_total_value,
    any_value(net_taxes) as net_taxes,
    any_value(special_assessment) as special_assessment,
    any_value(other_charges) as other_charges,
    any_value(total_taxes) as total_taxes,
    coalesce(any_value(lot_size), 0) as lot_size,
    any_value(frontage_street) as frontage_street,
    any_value(water_frontage) as water_frontage,
    any_value(tif_district) as tif_district,
    any_value(tax_school_dist) as tax_school_dist,
    any_value(elementary_school) as elementary_school,
    any_value(middle_school) as middle_school,
    any_value(high_school) as high_school,
    any_value(ward) as ward,
    any_value(state_assembly_district) as state_assembly_district,
    any_value(refuse_district) as refuse_district,
    any_value(refuse_url) as refuse_url,
    any_value(previous_land_2) as previous_land_2,
    any_value(previous_impr_2) as previous_impr_2,
    any_value(previous_total_2) as previous_total_2,
    any_value(alder_district) as alder_district,
    any_value(assessment_change_date) as assessment_change_date,
    any_value(building_district) as building_district,
    any_value(capitol_fire_district) as capitol_fire_district,
    any_value(date_added) as date_added,
    any_value(deed_page) as deed_page,
    any_value(deed_restriction) as deed_restriction,
    any_value(deed_volume) as deed_volume,
    any_value(electrical_district) as electrical_district,
    any_value(env_health_district) as env_health_district,
    any_value(exemption_type) as exemption_type,
    any_value(fire_district) as fire_district,
    any_value(flood_plain) as flood_plain,
    any_value(heating_district) as heating_district,
    any_value(illegal_land_division) as illegal_land_division,
    any_value(landscape_buffer) as landscape_buffer,
    any_value(local_historical_dist) as local_historical_dist,
    any_value(lot_depth) as lot_depth,
    any_value(lot_number) as lot_number,
    any_value(lot_type_1) as lot_type_1,
    any_value(lot_type_2) as lot_type_2,
    any_value(lot_width) as lot_width,
    any_value(national_historical_dist) as national_historical_dist,
    any_value(owner_occupied) as owner_occupied,
    any_value(parcel_change_date) as parcel_change_date,
    any_value(parcel_code) as parcel_code,
    any_value(pending) as pending,
    any_value(planning_district) as planning_district,
    any_value(plumbing_district) as plumbing_district,
    any_value(police_district) as police_district,
    any_value(police_sector) as police_sector,
    any_value(previous_class) as previous_class,
    any_value(property_use_code) as property_use_code,
    any_value(railroad_frontage) as railroad_frontage,
    any_value(senate_district) as senate_district,
    any_value(supervisor_district) as supervisor_district,
    any_value(total_dwelling_units) as total_dwelling_units,
    any_value(type_water_frontage) as type_water_frontage,
    any_value(zoning_all) as zoning_all,
    any_value(house_nbr) as house_nbr,
    any_value(street_dir) as street_dir,
    any_value(street_name) as street_name,
    any_value(street_type) as street_type,
    any_value(unit) as unit,
    any_value(property_change_date) as property_change_date,
    any_value(max_construction_year) as max_construction_year,
    any_value(x_coord) as x_coord,
    any_value(y_coord) as y_coord,
    ST_Union_Agg(geom) as geom,
    parcel_year
from {{ ref('stg_parcels') }}
group by parcel_id, parcel_year
